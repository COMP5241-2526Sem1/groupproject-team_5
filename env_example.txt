# Environment Variables Configuration
# Copy this file to .env and update the values

# Flask secret key for session encryption
SECRET_KEY=your-secret-key-here

# Database URL (SQLite for development, PostgreSQL for production)
DATABASE_URL=sqlite:///classroom.db

# ByteDance Ark API Key (optional - takes priority if provided)
ARK_API_KEY=your-bytedance-ark-api-key-optional

# OpenAI API Key (optional - used if Ark API is not available)
OPENAI_API_KEY=your-openai-api-key-optional

# Note: If neither ARK_API_KEY nor OPENAI_API_KEY is provided,
# the system will use fallback generation methods.

