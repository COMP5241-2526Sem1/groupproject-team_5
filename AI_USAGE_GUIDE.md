# 🤖 AI模块使用指南

## 📖 概述

您的QA教育平台集成了强大的AI功能，可以帮助教师智能生成问题、活动和分析学生回答。AI模块支持多种API服务，包括字节跳动Ark API和OpenAI API。

## 🔧 配置AI服务

### 1. 设置API密钥

在系统环境变量中设置API密钥：

```bash
# 方法1: 字节跳动Ark API (推荐)
export ARK_API_KEY="your_ark_api_key_here"

# 方法2: OpenAI API
export OPENAI_API_KEY="your_openai_api_key_here"

# 或者在.env文件中设置
echo "ARK_API_KEY=your_ark_api_key_here" >> .env
echo "OPENAI_API_KEY=your_openai_api_key_here" >> .env
```

### 2. API优先级
- **第一优先级**: Ark API (更便宜，中文支持更好)
- **第二优先级**: OpenAI API  
- **降级机制**: 如果API不可用，自动使用内置算法

## 🎯 AI功能模块

### 📝 智能问题生成

#### 功能说明
根据教学文本自动生成3个课堂互动问题，适合：
- 测试学生对核心概念的理解
- 鼓励批判性思维
- 适合简答或投票格式

#### 使用方法

##### 在创建活动页面使用：
1. **访问路径**: 课程详情 → 创建活动
2. **操作步骤**:
   ```
   1. 在"AI辅助生成"区域输入教学文本
   2. 点击"🤖 生成题目"按钮
   3. 等待AI生成3个问题
   4. 点击"使用此题"选择问题
   5. 继续完成活动创建
   ```

##### 通过API调用：
```javascript
// 前端调用示例
fetch('/activities/generate_questions', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
    },
    body: JSON.stringify({
        text: "你的教学文本内容"
    })
})
.then(response => response.json())
.then(data => {
    if (data.success) {
        console.log('生成的问题:', data.questions);
    }
});
```

##### 后端直接调用：
```python
from app.ai_utils import generate_questions

# 生成问题
questions = generate_questions("你的教学文本内容")
print(questions)  # ['问题1', '问题2', '问题3']
```

### 🎯 智能活动生成

#### 功能说明
根据教学内容生成完整的课堂活动，支持：
- **测验活动**: 多选题 + 正确答案 + 解释
- **投票活动**: 投票问题 + 选项
- **简答活动**: 开放性问题

#### 使用方法

##### API调用：
```javascript
fetch('/activities/generate_activity', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
    },
    body: JSON.stringify({
        content: "教学内容",
        activity_type: "quiz"  // 或 "poll", "short_answer"
    })
})
.then(response => response.json())
.then(data => {
    if (data.success) {
        console.log('生成的活动:', data.activity);
    }
});
```

##### 后端调用：
```python
from app.ai_utils import generate_activity_from_content

# 生成测验活动
activity_data = generate_activity_from_content(
    content="Python是一种编程语言...", 
    activity_type="quiz"
)

print(activity_data)
# {
#     "title": "Python基础测验",
#     "question": "关于Python的说法正确的是？",
#     "options": ["选项A", "选项B", "选项C", "选项D"],
#     "correct_answer": "选项A",
#     "explanation": "解释..."
# }
```

### 📊 智能答案分组

#### 功能说明
自动分析和分组学生的文本回答，识别相似观点和关键主题。

#### 使用方法

##### 在活动结果页面：
1. **访问路径**: 活动详情 → 查看结果
2. **操作步骤**:
   ```
   1. 进入活动结果页面
   2. 点击"智能分组答案"按钮
   3. AI自动分析所有学生回答
   4. 展示分组结果和关键词
   ```

##### API调用：
```javascript
fetch(`/activities/${activityId}/group_answers`, {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
    }
})
.then(response => response.json())
.then(data => {
    if (data.success) {
        console.log('分组结果:', data.grouped_data);
    }
});
```

## 🎨 界面使用指南

### 📝 创建活动时使用AI

```
┌─────────────────────────────────────────────────────────┐
│ 创建新活动                                              │
├─────────────────────────────────────────────────────────┤
│ 活动标题: [                              ]              │
│ 活动类型: [投票 ▼]                                      │
│ 活动问题: [                              ]              │
├─────────────────────────────────────────────────────────┤
│ 🤖 AI辅助生成                                           │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 请输入教学文本，AI将生成相关问题：                    │ │
│ │ [                                     ]              │ │
│ │                                                     │ │
│ │ [🤖 生成题目]                                        │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ 生成的题目：                                            │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 💡 关于Python编程的基本概念，下列说法哪个正确？        │ │
│ │ [使用此题]                                           │ │
│ └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 📊 活动结果分析

```
┌─────────────────────────────────────────────────────────┐
│ 活动结果分析                                            │
├─────────────────────────────────────────────────────────┤
│ 📈 统计信息: 参与人数 45 | 回答率 89%                   │
├─────────────────────────────────────────────────────────┤
│ [🤖 智能分组答案] [📊 导出结果] [🔄 实时更新]           │
│                                                         │
│ 🧠 AI分析结果：                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 主要观点1 (18人): "Python语法简单易学"               │ │
│ │ 主要观点2 (15人): "Python应用领域广泛"               │ │
│ │ 主要观点3 (12人): "Python社区生态完善"               │ │
│ └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

## 🔧 高级配置

### 自定义AI参数

您可以在`app/ai_utils.py`中调整AI参数：

```python
# Ark API配置
payload = {
    "model": "doubao-pro-1.5-32k",  # 模型选择
    "max_tokens": 500,              # 最大输出长度
    "temperature": 0.7              # 创造性参数 (0-1)
}

# OpenAI配置
response = openai.ChatCompletion.create(
    model="gpt-3.5-turbo",          # 模型选择
    max_tokens=500,                 # 最大输出长度
    temperature=0.7                 # 创造性参数 (0-1)
)
```

### 降级算法配置

当API不可用时，系统使用内置算法：

```python
def generate_questions_fallback(text: str) -> List[str]:
    # 基于文本分析的问题生成
    sentences = re.split(r'[.!?。！？]', text)
    # ... 算法逻辑
    return questions
```

## 🧪 测试AI功能

### 1. 快速测试脚本

创建`test_ai.py`文件：

```python
#!/usr/bin/env python3
import os
import sys
sys.path.append(os.path.dirname(os.path.abspath(__file__)))

from app.ai_utils import generate_questions, generate_activity_from_content

def test_ai_functions():
    test_text = """
    Python是一种高级编程语言，由Guido van Rossum在1989年发明。
    Python具有简洁的语法和强大的功能，广泛应用于Web开发、数据分析、
    人工智能等领域。Python的设计哲学强调代码的可读性。
    """
    
    print("🧪 测试AI问题生成功能...")
    questions = generate_questions(test_text)
    print("✅ 生成的问题:")
    for i, q in enumerate(questions, 1):
        print(f"   {i}. {q}")
    
    print("\n🧪 测试AI活动生成功能...")
    activity = generate_activity_from_content(test_text, "quiz")
    print("✅ 生成的活动:")
    print(f"   标题: {activity.get('title', 'N/A')}")
    print(f"   问题: {activity.get('question', 'N/A')}")
    print(f"   选项: {activity.get('options', 'N/A')}")

if __name__ == "__main__":
    test_ai_functions()
```

运行测试：
```bash
cd /Users/dududu/Desktop/QA_Platform/final_integrated_platform
python3 test_ai.py
```

### 2. Web界面测试

1. **启动应用**: `python3 run.py`
2. **登录管理员**: admin@example.com / admin123
3. **创建课程**: 访问课程管理
4. **测试AI功能**: 在创建活动页面测试AI生成

### 3. API端点测试

使用curl测试API：

```bash
# 测试问题生成
curl -X POST http://localhost:5001/activities/generate_questions \
  -H "Content-Type: application/json" \
  -d '{"text": "Python是一种编程语言..."}' \
  --cookie "session=your_session_cookie"

# 测试活动生成  
curl -X POST http://localhost:5001/activities/generate_activity \
  -H "Content-Type: application/json" \
  -d '{"content": "Python编程教学内容", "activity_type": "quiz"}' \
  --cookie "session=your_session_cookie"
```

## 🚨 常见问题

### Q1: AI生成失败怎么办？
**A**: 检查以下项目：
- ✅ API密钥是否正确设置
- ✅ 网络连接是否正常
- ✅ API余额是否充足
- ✅ 输入文本是否过长或过短

### Q2: 生成的问题质量不高？
**A**: 优化方法：
- 📝 提供更详细的教学文本
- 🎯 使用结构化的教学内容
- ⚙️ 调整temperature参数
- 🔄 多次生成选择最佳结果

### Q3: API调用速度慢？
**A**: 优化建议：
- 🌐 选择就近的API服务器
- ⚡ 减少max_tokens参数
- 🔄 使用异步调用
- 💾 缓存常用结果

### Q4: 如何自定义AI提示词？
**A**: 修改`ai_utils.py`中的system prompt：

```python
"content": "你是一位教育专家，请根据教学文本生成3个高质量的课堂互动问题..."
```

## 📚 相关文件

- **核心模块**: `app/ai_utils.py`
- **路由处理**: `app/routes/activities.py`
- **前端界面**: `templates/activities/create_activity.html`
- **测试文件**: `test_ai.py` (需要创建)

## 🎉 总结

AI模块为您的教育平台提供了：

✅ **智能问题生成**: 基于教学内容自动生成相关问题
✅ **多样化活动**: 支持测验、投票、简答等多种活动类型  
✅ **答案分析**: 智能分组和分析学生回答
✅ **降级保障**: API不可用时自动使用内置算法
✅ **灵活配置**: 支持多种AI服务和自定义参数

现在您可以充分利用AI功能来提升教学效率和学生参与度！🚀
